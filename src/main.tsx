import React from 'react'
import ReactDOM from 'react-dom/client'
import PromptScoreClassroomApp from './ui/PromptScoreClassroomApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PromptScoreClassroomApp />
  </React.StrictMode>,
)
